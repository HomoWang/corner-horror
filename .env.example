# Public WebSocket endpoint used by the GitHub Pages build.
VITE_WS_URL=wss://corner-horror-relay-homowang.onrender.com/ws

# Render server only. Use comma-separated origins when more than one frontend is allowed.
ALLOWED_ORIGINS=https://homowang.github.io
